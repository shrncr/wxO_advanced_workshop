[project]
name = "orchestrate_adk_labs"
version = "0.1.1"
description = "Become an AI Engineer with IBM watsonx Orchestrate's ' developer tools and AI Agent platform."
authors = [
    { name="shrnciar"},
    { name="dbennerIBM"},
    { name="Stiffler-IBM"},
    { name="sophiasouthard"}
]
readme = "README.md"
requires-python = ">=3.11,<3.14"

dependencies = [
    "requests",
    "pydantic",
    "ibm-watsonx-orchestrate>=1.12.0",
    "tavily-python>=0.7.11",
]

[tool.uv]
# optional: you can configure uv behavior here
